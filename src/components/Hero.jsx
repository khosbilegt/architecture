import React from 'react';
import { Profile } from '../res/images';
import './Hero.css';

function Hero(props) {
  return (
    <div className={`hero ${props.darkModeClass}`}>
      
    </div>
  )
}

export default Hero;